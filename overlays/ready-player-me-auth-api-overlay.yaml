overlay: 1.0.0
info:
  title: API Evangelist enhancements — Ready Player Me Auth API
  version: 1.0.0
x-provenance:
  generated: '2026-08-26'
  method: generated
  source: openapi/ready-player-me-auth-api-openapi.yml
  note: >-
    Overlay of API Evangelist enhancements. The original definition is never mutated. Repairs the
    info block copied from the Assets definition during the per-tag split, and marks the surface
    retired (public platform shut down 2026-01-31; api.readyplayer.me is NXDOMAIN).
extends: openapi/ready-player-me-auth-api-openapi.yml
actions:
  - target: $.info
    description: Correct the title and description carried over from the Assets definition during the per-tag split.
    update:
      title: Ready Player Me Auth API
      description: >-
        Anonymous user creation, email-code login (auth start / login), token refresh, and avatar
        access tokens used by the Ready Player Me Avatar Creator and SDKs. Requests are scoped to a
        studio application via the X-APP-ID header.
  - target: $.info
    description: Record the retirement of the public platform.
    update:
      x-lifecycle:
        status: retired
        service_end: '2026-01-31'
        reason: Public platform shut down following Netflix's December 2025 acquisition.
        host_resolves: false
        evidence: lifecycle/ready-player-me-lifecycle.yml
  - target: $.servers[0]
    description: Flag the production server as unreachable.
    update:
      description: Production (RETIRED — api.readyplayer.me returned NXDOMAIN on 2026-08-26)
